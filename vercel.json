{
  "version": 2,
  "buildCommand": "pnpm install --frozen-lockfile=false && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/revise run build",
  "outputDirectory": "artifacts/revise/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
